#!/usr/bin/env bash
# update packages and install virtual enviroment and pip
sudo apt-get update
sudo apt-get -y install python3-pip
# clone git repo, cd to it, craete virutal env, activate virutal env
sudo git clone https://github.com/BergeDios/Eventify.git